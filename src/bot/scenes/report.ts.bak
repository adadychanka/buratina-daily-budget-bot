import { Scenes } from "telegraf/scenes";
import { BotContext, ReportData } from "../../types/bot";
import { ReportSchema } from "../../types/report";

export const reportScene = new Scenes.BaseScene<BotContext>("report");

// Инициализация сцены
reportScene.enter(async (ctx) => {
  // Инициализируем данные отчета
  ctx.session = ctx.session || {};
  ctx.session.reportData = {};
  ctx.session.currentScene = "report";
  ctx.session.step = "cashAmount";

  await ctx.reply(
    "📊 Создание нового отчета\n\nВведи сумму наличных (Cash amount):"
  );
});

// Обработка текстовых сообщений
reportScene.on("text", async (ctx) => {
  const step = ctx.session?.step;
  const reportData = ctx.session?.reportData || {};

  try {
    switch (step) {
      case "cashAmount":
        await handleCashAmount(ctx, reportData);
        break;
      case "whiteCashAmount":
        await handleWhiteCashAmount(ctx, reportData);
        break;
      case "blackCashAmount":
        await handleBlackCashAmount(ctx, reportData);
        break;
      case "blackCashLocation":
        await handleBlackCashLocation(ctx, reportData);
        break;
      case "cardSalesAmount":
        await handleCardSalesAmount(ctx, reportData);
        break;
      case "expenses":
        await handleExpenses(ctx, reportData);
        break;
      case "cashboxAmount":
        await handleCashboxAmount(ctx, reportData);
        break;
      case "notes":
        await handleNotes(ctx, reportData);
        break;
      default:
        await ctx.reply("Неизвестный шаг. Начните заново командой /report");
    }
  } catch (error) {
    await ctx.reply("Произошла ошибка. Попробуйте еще раз.");
    console.error("Error in report scene:", error);
  }
});

// Обработчики для каждого шага
async function handleCashAmount(
  ctx: BotContext,
  reportData: Partial<ReportData>
) {
  const amount = parseFloat(ctx.message?.text || "0");

  if (isNaN(amount) || amount < 0) {
    await ctx.reply("❌ Пожалуйста, введи корректную сумму (число >= 0):");
    return;
  }

  reportData.cashAmount = amount;
  ctx.session!.step = "whiteCashAmount";

  await ctx.reply("✅ Сумма наличных сохранена\n\nВведи сумму White Cash:");
}

async function handleWhiteCashAmount(
  ctx: BotContext,
  reportData: Partial<ReportData>
) {
  const amount = parseFloat(ctx.message?.text || "0");

  if (isNaN(amount) || amount < 0) {
    await ctx.reply("❌ Пожалуйста, введи корректную сумму (число >= 0):");
    return;
  }

  reportData.whiteCashAmount = amount;
  ctx.session!.step = "blackCashAmount";

  await ctx.reply("✅ White Cash сохранен\n\nВведи сумму Black Cash:");
}

async function handleBlackCashAmount(
  ctx: BotContext,
  reportData: Partial<ReportData>
) {
  const amount = parseFloat(ctx.message?.text || "0");

  if (isNaN(amount) || amount < 0) {
    await ctx.reply("❌ Пожалуйста, введи корректную сумму (число >= 0):");
    return;
  }

  reportData.blackCashAmount = amount;
  ctx.session!.step = "blackCashLocation";

  await ctx.reply(
    '✅ Black Cash сохранен\n\nГде был сохранен Black Cash? (например: "Friday", "Monday"):'
  );
}

async function handleBlackCashLocation(
  ctx: BotContext,
  reportData: Partial<ReportData>
) {
  const location = ctx.message?.text?.trim();

  if (!location) {
    await ctx.reply("❌ Пожалуйста, введи место хранения Black Cash:");
    return;
  }

  reportData.blackCashLocation = location;
  ctx.session!.step = "cardSalesAmount";

  await ctx.reply(
    "✅ Место хранения Black Cash сохранено\n\nВведи сумму продаж по карте:"
  );
}

async function handleCardSalesAmount(
  ctx: BotContext,
  reportData: Partial<ReportData>
) {
  const amount = parseFloat(ctx.message?.text || "0");

  if (isNaN(amount) || amount < 0) {
    await ctx.reply("❌ Пожалуйста, введи корректную сумму (число >= 0):");
    return;
  }

  reportData.cardSalesAmount = amount;
  reportData.expenses = [];
  ctx.session!.step = "expenses";

  await ctx.reply(
    '✅ Продажи по карте сохранены\n\nХочешь добавить расходы? (да/нет или "skip" для пропуска):'
  );
}

async function handleExpenses(
  ctx: BotContext,
  reportData: Partial<ReportData>
) {
  const text = ctx.message?.text?.toLowerCase().trim();

  if (text === "skip" || text === "нет" || text === "no") {
    ctx.session!.step = "cashboxAmount";
    await ctx.reply(
      "✅ Расходы пропущены\n\nВведи сумму в кассе в конце смены:"
    );
    return;
  }

  if (text === "да" || text === "yes") {
    await ctx.reply("Введи сумму расхода:");
    return;
  }

  // Если это число, обрабатываем как расход
  const amount = parseFloat(ctx.message?.text || "0");
  if (!isNaN(amount) && amount > 0) {
    // TODO: Добавить логику для обработки расходов
    await ctx.reply("✅ Расход добавлен\n\nВведи описание расхода:");
    return;
  }

  await ctx.reply('❌ Пожалуйста, ответь "да", "нет" или "skip":');
}

async function handleCashboxAmount(
  ctx: BotContext,
  reportData: Partial<ReportData>
) {
  const amount = parseFloat(ctx.message?.text || "0");

  if (isNaN(amount) || amount < 0) {
    await ctx.reply("❌ Пожалуйста, введи корректную сумму (число >= 0):");
    return;
  }

  reportData.cashboxAmount = amount;
  ctx.session!.step = "notes";

  await ctx.reply(
    '✅ Сумма в кассе сохранена\n\nДобавь примечания (или "skip" для пропуска):'
  );
}

async function handleNotes(ctx: BotContext, reportData: Partial<ReportData>) {
  const text = ctx.message?.text?.trim();

  if (text && text.toLowerCase() !== "skip") {
    reportData.notes = text;
  }

  // Рассчитываем общую сумму продаж
  const totalSales =
    (reportData.cashAmount || 0) +
    (reportData.whiteCashAmount || 0) +
    (reportData.blackCashAmount || 0) +
    (reportData.cardSalesAmount || 0);

  reportData.totalSales = totalSales;

  // Показываем сводку и запрашиваем подтверждение
  await showReportSummary(ctx, reportData as ReportData);
}

async function showReportSummary(ctx: BotContext, reportData: ReportData) {
  const summary = `
📊 Сводка отчета:

💰 Наличные: ${reportData.cashAmount}
💳 White Cash: ${reportData.whiteCashAmount}
🖤 Black Cash: ${reportData.blackCashAmount} (${
    reportData.blackCashLocation || "N/A"
  })
💳 Продажи по карте: ${reportData.cardSalesAmount}
📦 Расходы: ${reportData.expenses?.length || 0} шт.
💰 В кассе: ${reportData.cashboxAmount}
📝 Примечания: ${reportData.notes || "Нет"}

📈 Общая сумма продаж: ${reportData.totalSales}

Подтвердить и отправить отчет? (да/нет)
  `;

  await ctx.reply(summary);
  ctx.session!.step = "confirmation";
}

// Обработка подтверждения
reportScene.on("text", async (ctx) => {
  if (ctx.session?.step === "confirmation") {
    const text = ctx.message?.text?.toLowerCase().trim();

    if (text === "да" || text === "yes") {
      // TODO: Сохранить отчет в базу данных и Google Sheets
      await ctx.reply("✅ Отчет успешно отправлен!");
      await ctx.scene.leave();
    } else if (text === "нет" || text === "no") {
      await ctx.reply("❌ Отчет отменен. Начните заново командой /report");
      await ctx.scene.leave();
    } else {
      await ctx.reply('❌ Пожалуйста, ответь "да" или "нет":');
    }
  }
});
